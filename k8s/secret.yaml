# ─────────────────────────────────────────────────────────────────────────────
# Secrets Template
# IMPORTANT: Do NOT commit real secrets! Use Azure Key Vault or sealed secrets.
# This file is a template - replace placeholders before applying.
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: postmortem-secrets
  namespace: postmortem
type: Opaque
stringData:
  # Replace these with actual values (or use external-secrets/vault)
  COSMOS_ENDPOINT: "https://your-cosmos.documents.azure.com:443/"
  COSMOS_KEY: "your-cosmos-key"
  GROQ_API_KEY: "your-groq-api-key"
  APPINSIGHTS_INSTRUMENTATIONKEY: "your-app-insights-key"

